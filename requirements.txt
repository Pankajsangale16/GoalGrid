# Core dependencies
Django>=4.2.7,<5.0
django-allauth>=0.58.0
python-dotenv>=1.0.0
Pillow>=10.0.0
whitenoise>=6.6.0
psycopg2-binary>=2.9.9  # Required for PostgreSQL
gunicorn>=21.2.0

# Development dependencies (not needed in production)
python-dotenv>=1.0.0
